% LaTeX header-include for README.pdf (see docs/BUILDCHAIN.md's "PDF
% generation" note and tools/pandoc_wrap_code.lua). Provides \seqsplit,
% used by that Lua filter to make long unbreakable inline-code tokens
% (paths, URLs) wrap instead of overflowing the page margin.
\usepackage{seqsplit}
